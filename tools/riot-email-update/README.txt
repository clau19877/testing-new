BUILD 2026-08-09n

This zip DOES NOT contain data/tasks.csv. Keep your existing file.

After Sign in, the script now proceeds to the email field as soon as the
account page is ready (no long 2-minute wait / unnecessary reload).

Update and run:
  cd ~/Desktop/"riot-email-update-safari-mac 3"
  chmod +x *.sh *.command fetch_riot_imap_code.py fetch_riot_verify_link.py
  ./run_safari_mac.sh

Confirm BUILD 2026-08-09n in debug/logs/safari_*.log
