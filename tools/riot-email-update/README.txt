BUILD 2026-08-09l

This zip DOES NOT contain data/tasks.csv. Keep your existing file.

Logout step:
  1) button[data-testid=log-out-everywhere-button]  (LOG OUT EVERYWHERE)
  2) button[data-testid=modal_close-btn]            (Confirm)

Update and run:
  cd ~/Desktop/"riot-email-update-safari-mac 3"
  chmod +x *.sh *.command fetch_riot_imap_code.py fetch_riot_verify_link.py
  ./run_safari_mac.sh

Confirm BUILD 2026-08-09l in debug/logs/safari_*.log
