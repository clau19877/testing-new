BUILD 2026-08-12m
Password change itself can succeed (form_cleared) while post-change
re-login verify hangs (timeout / no-signin). Harden Sign in click,
settle after logout, and accept UI-cleared when old password no longer
restores the session.
Do NOT open .applescript in Script Editor — use RUN_ME.command.
Zip does not include data/tasks.csv.
