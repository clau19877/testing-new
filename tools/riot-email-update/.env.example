# Optional — leave blank to be prompted interactively (recommended).
# Never commit a real .env file.

RIOT_USERNAME=
RIOT_PASSWORD=
NEW_EMAIL=

# true = show browser (recommended; captcha / MFA often required)
# false = headless (often fails on Riot login)
HEADED=true
